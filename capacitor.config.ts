import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beaversoft.tasklist',
  appName: 'beaver-lista-de-tareas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
